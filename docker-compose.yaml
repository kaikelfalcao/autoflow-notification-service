version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: notification-mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: notification
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: