apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: autoflow
spec:
  selector:
    app: notification-service
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
      name: http
  type: ClusterIP
